{
    "name": "profile",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "nuxt dev -p ${PORT:-3002}",
        "build": "nuxt build",
        "start": "nuxt preview -p ${PORT:-3002}",
        "start:prod": "nuxt start -p ${PORT:-3002}"
    },
    "dependencies": {
        "nuxt": "^3.0.0",
        "pinia": "^2.0.0"
    }
}

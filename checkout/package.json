{
    "name": "checkout",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "nuxt dev -p ${PORT:-3001}",
        "build": "nuxt build",
        "start": "nuxt preview -p ${PORT:-3001}",
        "start:prod": "nuxt start -p ${PORT:-3001}"
    },
    "dependencies": {
        "nuxt": "^3.0.0",
        "pinia": "^2.0.0"
    }
}

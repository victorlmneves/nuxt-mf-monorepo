{
    "name": "admin",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "nuxt dev -p ${PORT:-3003}",
        "build": "nuxt build",
        "start": "nuxt preview -p ${PORT:-3003}",
        "start:prod": "nuxt start -p ${PORT:-3003}"
    },
    "dependencies": {
        "nuxt": "^3.21.6",
        "pinia": "^2.0.0"
    }
}
